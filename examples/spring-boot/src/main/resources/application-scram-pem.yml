spring:
  kafka:
    security:
      protocol: SASL_SSL
    ssl:
      trust-store-type: PEM
      trust-store-location: file:${KAFKA_CA_PEM}
    properties:
      "[sasl.mechanism]": SCRAM-SHA-512
      "[sasl.jaas.config]": >-
        org.apache.kafka.common.security.scram.ScramLoginModule required
        username="${KAFKA_USERNAME}"
        password="${KAFKA_PASSWORD}";
